# ВАЖНО: комментарии только на отдельных строках — некоторые хостинги
# читают файл построчно и передают строку в pip как есть.

# Бизнес-обновления (business_connection, business_message) появились в
# Bot API 7.2 и в aiogram 3.7. На 3.20+ приезжает ещё BusinessBotRights
# с раздельными правами бота — код умеет и то, и то.
aiogram>=3.15,<4
aiosqlite>=0.20,<0.22
python-dotenv>=1.0,<2.0
